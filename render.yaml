# Render Blueprint - https://render.com/docs/blueprint-spec
services:
  - type: web
    name: notesapp
    runtime: docker
    plan: free
    healthCheckPath: /
    disk:
      name: notesapp-data
      mountPath: /var/www/html/data
      sizeGB: 1
